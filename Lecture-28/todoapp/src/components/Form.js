import React from 'react'

const Form = () => {
    return (
        <form>
            <input type="text" placeholder='ADD to do here' />
            <button type="submit">ADD</button>

        </form>
    )
}

export default Form
